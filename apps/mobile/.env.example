# Base URL of the duit-cards-backend API, including the /api/v1 prefix.
# Use your Mac's LAN IP (not localhost) when testing on a physical device over Wi-Fi,
# e.g. `ipconfig getifaddr en0` on macOS.
EXPO_PUBLIC_API_URL=http://localhost:4000/api/v1
