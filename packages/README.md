# Packages

Shared packages will live here as the monorepo grows.

Good candidates:

- `@duit/shared` — shared TypeScript types and validation schemas.
- `@duit/api-client` — generated or hand-written API client helpers.
- `@duit/config` — shared linting, TypeScript, and build configuration.
